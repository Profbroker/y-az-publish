# Как добавить новую яхту

Пошаговый чек-лист. Время: ~15 минут.

---

## 1. Подготовить фото

В Tilda загрузить все фото яхты:
- Tilda → **Файлы и медиа** → загрузить
- Скопировать URL-ы (через `webp`-оптимизацию: `https://optim.tildacdn.com/.../format/webp/...`)

Нужно:
- **1 главное фото** — для карточки в каталоге и hero на странице (горизонтальное, желательно с водой)
- **3-5 фото для галереи** — экстерьер, кокпит, интерьер, общий план

---

## 2. Добавить строку в `catalog/data/yachts.csv`

Открыть https://github.com/Profbroker/y-az-publish/blob/main/catalog/data/yachts.csv → нажать карандаш «Edit» → добавить строку.

Поля (29 колонок, в порядке):

| Поле | Что писать | Пример |
|---|---|---|
| `slug` | URL-сегмент: **латиница строчными**, без пробелов | `caspian` |
| `name` | Имя яхты для UI | `Caspian` |
| `shipyard` | Верфь | `Tuzla` |
| `model` | Модель | `Custom` |
| `type_ru` / `_en` / `_az` | Тип ×3 языка | `Моторная` / `Power` / `Motor` |
| `pa_low_h` | Кол-во часов длинного пресета | `4` |
| `pa_mid_h` | Кол-во часов **популярного** пресета (выделен в UI) | `2` |
| `pa_high_h` | Кол-во часов короткого пресета | `1` |
| `desc_short_ru` / `_en` / `_az` | 1-2 предложения для карточки. **В кавычках если есть запятая** | `"Моторная яхта для активного дня в бухте."` |
| `desc_full_ru` / `_en` / `_az` | Абзац 3-6 предложений для страницы яхты | `"Caspian — моторная яхта..."` |
| `photo_main` | Один URL главного фото | `https://optim.tildacdn.com/.../webp` |
| `photos` | Несколько URL через `\|` (pipe) | `https://...\|https://...\|https://...` |
| `length` | С единицей | `15 m` |
| `beam` | С единицей | `4 m` |
| `guests` | int | `10` |
| `marina ` (с пробелом!) | Район Баку (не указывать конкретную марину публично) | `Baku Yacht Club` |
| `speed` | С единицей | `15 kn` |
| `engine` | Текст | `Yanmar 280` |
| `cabins` | int | `2` |
| `shower` / `toilet` / `platform` | да / нет | `да` |
| `price_from` | int, AZN (fallback для UI если pa_* пусты) | `300` |
| `active` | `yes` или `no` (если `no` — не в каталоге) | `yes` |
| `url_ru` / `_en` / `_az` | Полные URL'ы страниц на yacht.az | `https://yacht.az/ru/yachts/caspian` |

⚠️ **Slug — НЕ менять после публикации.** Сломает URL и SEO.

Внизу — кнопка **«Commit directly to the main branch»** → Commit changes.

---

## 3. Добавить строки в `catalog/data/prices.csv`

Открыть https://github.com/Profbroker/y-az-publish/blob/main/catalog/data/prices.csv → Edit.

**Если яхта сезонная** (цена high-сезон ≠ low-сезон) → **две строки**: одна `season=high`, вторая `season=low`.
**Если фикс-цена** → одна строка, `seasonal=no`, `season` и `season_emoji` пустые.

Поля:

| Поле | Что | Пример (high) | Пример (low) |
|---|---|---|---|
| `id` | Имя яхты | `Caspian` | `Caspian` |
| `name` | То же | `Caspian` | `Caspian` |
| `emoji` | Пустое | | |
| `slug` | То же что в yachts.csv | `caspian` | `caspian` |
| `fee` | Доп. fee (плюсуется к total) | `50` | `50` |
| `seasonal` | `yes` если две строки | `yes` | `yes` |
| `season` | `high` или `low` | `high` | `low` |
| `season_emoji` | для UI | `🌞` | `🍂` |
| `h1`..`h9` | Ставка за **каждый** последовательный час (AZN/час) | `500,250,250,150,150,120,120,120,120` | (со скидками) |
| `discount_start` | С какого часа начинается скидка | `2` | `2` |
| `url_ru` / `_en` / `_az` | URL страниц | `/ru/yachts/caspian` | то же |

Commit → main.

---

## 4. Подождать ~1-2 минуты

CDN (rawcdn.githack.com) пропагирует свежий CSV на edges. Можно сразу идти в Tilda.

---

## 5. Создать 3 страницы в Tilda

Tilda → структура сайта → создать **три новые страницы**:
- `/ru/yachts/<slug>`
- `/en/yachts/<slug>`
- `/az/yachts/<slug>`

(Точно тот же slug что и в CSV.)

На каждой добавить **alias-блок** на master-T123 (тот же что для Alora/Caspian/Amina). Master лежит на скрытой странице — он один на все яхты, не дублировать.

---

## 6. Опубликовать

Каждую из трёх страниц → **Опубликовать**.

---

## 7. Проверить

В **инкогнито** (важно — обходит браузерный кэш):
- `https://yacht.az/ru/yachts/<slug>` — есть hero, описание, цены, фото, кнопки
- `https://yacht.az/en/yachts/<slug>` — то же на английском
- `https://yacht.az/az/yachts/<slug>` — то же на азербайджанском
- `https://yacht.az/ru/yachts` — новая яхта появилась в общем каталоге

Если что-то не отображается:
- F12 → Console — есть ли ошибки
- F12 → Network → ввести `csv` — должны быть запросы к `rawcdn.githack.com`
- Проверить что в CSV не съехали колонки (запятая внутри текста без кавычек ломает парсинг)

---

## Если меняешь только данные существующей яхты

Шаги 2-3 (правка CSV) и подождать ~1 мин. В Tilda ничего трогать не надо.

## Если убираешь яхту с сайта

В `yachts.csv` → колонка `active` → `no`. Push. Яхта исчезнет из каталога. Страница в Tilda продолжит существовать (можно вручную скрыть или поставить 301-редирект).
